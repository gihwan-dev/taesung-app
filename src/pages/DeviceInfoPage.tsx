import AppBar from "../components/AppBar";

const DeviceInfoPage = () => {
  return (
    <section>
      <AppBar
        returnArrow
        selection
        settings
      />
    </section>
  );
};

export default DeviceInfoPage;
